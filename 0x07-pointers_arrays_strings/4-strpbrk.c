#include "main.h"

/**
 * _strpbrk - searches a string for any of a set of bytes
 * locates the first occurence in the string s of any of the byte in the string *accept
 * @*accept: pointer of the destination substring
 * @*s: pointer to the source string
 *
 * Return: The pointer to the byte in s that matches one of the bytes in accept, or NULL if no such byte is found
 */
char *_strpbrk(char *s, char *accept)

{
	return (accept);
}
