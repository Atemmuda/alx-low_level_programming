#include "main.h"
/**
 * _strstr -locate a substring
 * finds the first occurence of substring needle in the string haystack. The null byte (\o) are not compared
 * @*haystack: pointer of the destination substring
 * @*needle: pointer to the source string
 *
 * Return: THe beginning of the located substring, or NULL if the substring is not found
 */
 
char * _strstr(char *haystack, char *needle)
{
	return (needle);
}
