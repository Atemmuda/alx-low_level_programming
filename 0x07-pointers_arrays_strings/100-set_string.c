#include "main.h"

/**
 * set_string - set the value to a char
 * @s: contains the string to be pointed to
 * @to: the pointer dereferencer
 *
 * Return: Always successful (0)
 */ 

void set_string(char **s, char *to)
{
	return (0);
}
