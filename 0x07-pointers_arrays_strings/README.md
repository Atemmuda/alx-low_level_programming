# Concepts
* Pointers and Arrays

File | Description
---- | -----------
0-memset | Contains the _memset() function fills the first n bytes of the memory area pointed to by s with the constant byte b.
1-memcpy.c | contains a function that copies memory area.
2-strchr.c | Contains a function that return a character in a string
3-strspn.c | Contains a function that that gets the length of a prefix substring
4-strpbrk.c | Contains the function that for any of a set of byte.
5-strstr.c | Contain the funtion that locate a substring in a main string
7-print_chessboard.c | Print the pattern of the chess board
8-print_diagsums.c | Contains a function that print the sum of two diagonals of a square matrix of integers
100-set_string.c | COntains a funtion that sets the value of a pointer to char
101-crackme_password | Create a file that contains the password for the crackme2 executable.wq
